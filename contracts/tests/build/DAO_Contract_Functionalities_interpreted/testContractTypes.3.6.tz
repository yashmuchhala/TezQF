Storage: sp.TRecord(administrator = sp.TAddress, balances = sp.TBigMap(sp.TAddress, sp.TRecord(approvals = sp.TMap(sp.TAddress, sp.TNat), balance = sp.TNat).layout(("approvals", "balance"))), daoContract = sp.TAddress, paused = sp.TBool, totalSupply = sp.TNat).layout((("administrator", "balances"), ("daoContract", ("paused", "totalSupply"))))
Params: sp.TVariant(approve = sp.TRecord(spender = sp.TAddress, value = sp.TNat).layout(("spender", "value")), mint = sp.TRecord(address = sp.TAddress, value = sp.TNat).layout(("address", "value")), setAdministrator = sp.TRecord(newAdmin = sp.TAddress).layout("newAdmin"), setPause = sp.TRecord(pause = sp.TBool).layout("pause"), transfer = sp.TRecord(from_ = sp.TAddress, to_ = sp.TAddress, value = sp.TNat).layout(("from_", ("to_", "value")))).layout((("approve", "mint"), ("setAdministrator", ("setPause", "transfer"))))