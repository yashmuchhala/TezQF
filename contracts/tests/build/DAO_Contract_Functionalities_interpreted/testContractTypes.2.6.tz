Storage: sp.TRecord(currentRound = sp.TNat, daoContractAddress = sp.TAddress, isRoundActive = sp.TBool, rounds = sp.TBigMap(sp.TNat, sp.TRecord(end = sp.TTimestamp, entries = sp.TBigMap(sp.TNat, sp.TRecord(address = sp.TAddress, contributions = sp.TList(sp.TRecord(address = sp.TAddress, amount = sp.TNat, clrMatch = sp.TMutez).layout(("address", ("amount", "clrMatch")))), description = sp.TString, disputeEnd = sp.TTimestamp, disputed = sp.TBool, disqualified = sp.TBool, sponsorshipWon = sp.TMutez, subsidyPower = sp.TNat, totalContribution = sp.TMutez).layout(((("address", "contributions"), ("description", "disputeEnd")), (("disputed", "disqualified"), ("sponsorshipWon", ("subsidyPower", "totalContribution")))))), entryId = sp.TNat, name = sp.TString, sponsors = sp.TMap(sp.TAddress, sp.TRecord(amount = sp.TMutez, name = sp.TString).layout(("amount", "name"))), start = sp.TTimestamp, totalContribution = sp.TMutez, totalSponsorship = sp.TMutez, totalSubsidyPower = sp.TNat).layout(((("end", "entries"), ("entryId", "name")), (("sponsors", "start"), ("totalContribution", ("totalSponsorship", "totalSubsidyPower"))))))).layout((("currentRound", "daoContractAddress"), ("isRoundActive", "rounds")))
Params: sp.TVariant(contribute = sp.TRecord(entryId = sp.TNat).layout("entryId"), createNewRound = sp.TRecord(end = sp.TTimestamp, name = sp.TString, sponsors = sp.TMap(sp.TAddress, sp.TRecord(amount = sp.TMutez, name = sp.TString).layout(("amount", "name"))), start = sp.TTimestamp, totalSponsorship = sp.TMutez).layout((("end", "name"), ("sponsors", ("start", "totalSponsorship")))), disburse = sp.TUnit, dispute = sp.TRecord(entryId = sp.TNat).layout("entryId"), disqualify = sp.TRecord(entryId = sp.TNat).layout("entryId"), enterRound = sp.TRecord(description = sp.TString).layout("description")).layout((("contribute", ("createNewRound", "disburse")), ("dispute", ("disqualify", "enterRound"))))